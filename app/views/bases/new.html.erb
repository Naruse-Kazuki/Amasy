<% provide(:title, 'Base new') %>
<% provide(:class_text, 'base_new') %>
<% provide(:button_text, '拠点追加') %>

<h1>拠点追加</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(model: @base, local: true) do |f| %>
      <%= render 'form', f: f %>
    <% end %>
  </div>
</div>